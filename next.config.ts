import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'i.pinimg.com'], // Add your image domains here
  },
  /* config options here */
};

export default nextConfig;
